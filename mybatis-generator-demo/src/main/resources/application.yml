spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.driver
    url: jdbc:mysql://localhost:3306/ocean?useSSL=false
    username: root
    password: 123456

mybatis:
  mapper-locations: classpath:/mybatis/mapper/*.xml
  type-aliases-package: io.github.hizhangbo.model